import { ParseHtmlDirective } from './parse-html.directive';

describe('ParseHtmlDirective', () => {
  it('should create an instance', () => {
    // const directive = new ParseHtmlDirective();
    // expect(directive).toBeTruthy();
  });
});
 